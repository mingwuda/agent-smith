blog/
__pycache__/
*.pyc
.DS_Store
agent.log
.workbuddy/
*.egg-info/
dist/
build/
.venv/
.venv-windows-build/
.venv-windows-verify/
.venv-macos-build/
# Python 升级时的旧 venv 备份，体积大，不入仓
.venv_py39_bak
.pip-cache/
.playwright-browsers/
venv/
.env

# 调试/构建产物（本地临时，不入仓）
.browser_screenshots/
build_*.log

# Electron（node_modules 含 Electron 运行时二进制，体积大，由 npm install 重新生成）
electron/node_modules/
# electron-builder 产物统一输出到 dist/electron/（已被上方 dist/ 忽略）
# electron-builder 打包时生成在仓库根的空缓存文件（忽略即可）
/electron-builder
tmp_verify/
run_test.log
run_verify.log
defender_check.txt
procs.txt
*.db
*.sqlite3
